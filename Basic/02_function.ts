function addTwo(number: number) {
  return number + 2;
}

function getUpper(val: string) {
  return val.toUpperCase();
}

function login(name: string, email: string, isPaid: boolean = false) {}

addTwo(4);
getUpper("ankush");
login("ankush", "a2a.com");

export {};
